let display;

function setup() {
    createCanvas(1500, 980);
    display = new Display();
}

///////////// (j * 10 + i) = row number plus x position
function draw() {
    background(255);
    rectMode(CENTER);
    stroke(0);
}

